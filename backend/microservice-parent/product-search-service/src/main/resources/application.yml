server:
  port: 0

spring:
  application:
    name: product-search-service
  data:
    elasticsearch:
      client:
        elasticsearch:
          hosts: http://elasticsearch:9200   # use http, not https
          username: elastic
          password: changeme

eureka:
  instance:
    instance-id: ${spring.application.name}:${random.value}
    prefer-ip-address: true
  client:
    serviceUrl:
      defaultZone: http://eureka:password@localhost:8761/eureka
